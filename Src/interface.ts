type user={
    name:string;
    age:number;
}

interface Iuser{
    name:string;
    age:number;
}

type role={
    role:"admin "| "user"
}
const user1:user={
    name:"mr.s",
    age:100,
};
const user2:user={
    name:"mr.y",
    age:103,
}